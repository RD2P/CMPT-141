def improvedAverage(L):
    """ returns a boolean value indicating whether the average of the last 10 values in L
    is greater than the average of the rest of the values in L
    
    :params:
        L: (list) a list of integers or floats
        
    :return:
        (boolean) True if the average of the last 10 values is greater than the average of the rest
        False if its not
        None if the length of L is less than 20.
    """
    # this is a dummy version of this function. it just always returns False
    # because we're practicing blackbox testing, we don't get to see the actual function
    # but this placeholder will let us test if the SYNTAX of the test driver is correct
    return False